model_params:
  moving_avg: 25
  model_dim: 128
  dropout: 0.1
  num_heads: 8
  fcn_dim: 64
  encoder_layers: 3
  modes: 32
transforms:
  train:
    window:
      args:
        window_size: 50
  test:
    window:
      args:
        window_size: 50
  seq_len: 50  # Based on the window size of the training data

