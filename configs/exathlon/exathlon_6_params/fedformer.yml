model_params:
  moving_avg: 25
  model_dim: 128
  dropout: 0.1
  num_heads: 8
  fcn_dim: 128
  encoder_layers: 3
  modes: 32
transforms:
  train:
    window:
      args:
        window_size: 10
  test:
    window:
      args:
        window_size: 10
  seq_len: 10  # Based on the window size of the training data

